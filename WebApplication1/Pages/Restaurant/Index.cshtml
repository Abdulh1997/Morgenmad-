@page
@model WebApplication1.Pages.Restaurant.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Here you can check guests in for breakfast</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CheckedIn[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CheckedIn[0].NumberOfAdults)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CheckedIn[0].NumberOfChildren)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CheckedIn[0].RoomNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CheckedIn) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumberOfAdults)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumberOfChildren)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoomNumber)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CheckedInId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CheckedInId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CheckedInId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
